{
  "name": "code-challenge",
  "version": "1.0.0",
  "description": "code challenge for MATTR",
  "main": "jest.config.js",
  "scripts": {
    "format": "prettier --write \"*.ts\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Steven Zhang",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^29.5.3",
    "jest": "^29.6.2",
    "prettier": "^3.0.1",
    "ts-jest": "^29.1.1"
  }
}